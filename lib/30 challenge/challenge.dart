import 'package:flutter/material.dart';

class challenge extends StatelessWidget {
  const challenge({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(child: Text("30 challenge"))
    );
  }
}

