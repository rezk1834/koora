import 'package:flutter/material.dart';

class aqua extends StatelessWidget {
  const aqua({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(child: Text("Aqua ta7ady"))
    );
  }
}
